export interface IUserState {
  isUserSignupLoading: boolean;
  isUserSignupSuccess: boolean;
  isUserSignupError: boolean;
  userDetails: any;
  isUserLoginLoading: boolean;
  isUserLoginSuccess: boolean;
  isUserLoginError: boolean;
}

export interface IUserDetails {}
